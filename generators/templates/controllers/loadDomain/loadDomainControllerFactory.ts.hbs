import { makeLogController } from "@/application/decorators/logControllerFactory";
import { makeValidationComposite } from "@/application/factories";
import { Controller } from "@/application/infra/contracts";
import { makeLoad{{pascalCase name}}Factory } from "@/slices/{{camelCase name}}/useCases";
import { Load{{pascalCase name}}Controller } from "@/slices/{{camelCase name}}/controllers";

export const makeLoad{{pascalCase name}}Controller = (): Controller => {
  const requiredFields = ["_id"];
  return makeLogController(
    "load{{pascalCase name}}",
    new Load{{pascalCase name}}Controller(
      makeValidationComposite(requiredFields),
      makeLoad{{pascalCase name}}Factory()
    )
  );
};
