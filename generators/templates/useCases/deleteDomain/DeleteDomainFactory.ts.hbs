import { makeDatabaseInstance } from "@/application/infra";
import { whiteLabel } from "@/application/infra/config/whiteLabel";
import { {{pascalCase name}}Repository } from "@/slices/{{camelCase name}}/repositories";
import { delete{{pascalCase name}}, Delete{{pascalCase name}} } from "@/slices/{{camelCase name}}/useCases";

export const makeDelete{{pascalCase name}}Factory = (): Delete{{pascalCase name}} => {
  const repository = new {{pascalCase name}}Repository(makeDatabaseInstance(whiteLabel.database,"{{camelCase name}}"));
  return delete{{pascalCase name}}(repository);
};
