import { health } from "./health";
import { user } from "./user";
import { auth } from "./auth";
// IMPORT MODULE FILES
const routes = [
  health,
  user,
  auth,
  // ADD FUNCTION IMPORTS
];

export { routes };
